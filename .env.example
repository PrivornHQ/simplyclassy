# Required for /admin (set as a Cloudflare Worker secret — do not commit real values)
ADMIN_PASSWORD=

# Optional. 32+ characters recommended. If omitted, a key is derived from ADMIN_PASSWORD.
ADMIN_SESSION_SECRET=